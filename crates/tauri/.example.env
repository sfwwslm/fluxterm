# FluxTerm 环境变量示例
# 用法：复制本文件为 .env 后按需修改。
# 说明：配置目录必须位于 $HOME 下。
# - 开发环境建议使用相对路径（自动解析到用户主目录）。
# - 生产环境如使用绝对路径，也必须是 $HOME 下的绝对路径。

# 应用配置目录（优先级高于默认 ~/.vust/flux-term）
# - 绝对路径：仅允许 $HOME 下路径。
# - 相对路径：会自动解析为 $HOME/<relative-path>。
FLUXTERM_CONFIG_DIR=.vust/flux-term-dev
