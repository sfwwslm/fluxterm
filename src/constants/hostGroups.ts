/** 内置默认 SSH 分组值（用于 UI 选择，不直接存储到 profile.tags）。 */
export const DEFAULT_SSH_GROUP_VALUE = "__default_ssh__";

/** 内置本地 Shell 分组标识（仅用于前端分组渲染）。 */
export const LOCAL_SHELL_GROUP_VALUE = "__local_shells__";
