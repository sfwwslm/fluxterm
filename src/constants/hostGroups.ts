/** 根级会话占位值（仅用于前端分组选择，不直接存储到 profile.tags）。 */
export const ROOT_PROFILE_GROUP_VALUE = "__root_profiles__";

/** 内置本地 Shell 分组标识（仅用于前端分组渲染）。 */
export const LOCAL_SHELL_GROUP_VALUE = "__local_shells__";
